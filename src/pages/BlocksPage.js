import React, { Component } from 'react';
import { BlocksPageContainer } from '../containers'

class BlocksPage extends Component {
  render() {
    return (
      <BlocksPageContainer />
    );
  }
}

export default BlocksPage;
